<Window x:Class="DynamicWhereClauseExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        xmlns:local="clr-namespace:DynamicWhereClauseExample"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <local:SearchCriteriaView DataContext="{Binding SearchCriteria}" Grid.Row="0" />
        <Grid Grid.Row="1" Margin="5">
            <Label Content="Results" HorizontalAlignment="Left" />
            <Button Content="Search" HorizontalAlignment="Right" Padding="5" />
        </Grid>
        
        <DataGrid Grid.Row="2" ItemsSource="{Binding SearchResults}" />
    </Grid>
</Window>
